Componentes com IA: O que incluir
1. Reconhecimento de Entidades Nomeadas (NER) com spaCy
Para identificar nomes de pessoas, empresas, localizações e relacionar dados pessoais mesmo sem padrão fixo.

2. Análise semântica com modelos como GPT ou Llama via LangChain (opcional)
Para interpretar o contexto quando regex e NER falham (ideal para contratos complexos).

3. Classificação automática de tipo de dado e associação com titular usando IA.
📦 Estrutura Completa com IA no Prompt
💬 Atualize o projeto Python no Replit com os seguintes aprimoramentos de IA:

Adicione:
Integração com spaCy para fazer Reconhecimento de Entidades Nomeadas (NER), incluindo nomes de pessoas, empresas e organizações.

Use o modelo pt_core_news_lg para português.

Após a extração de dados pessoais com regex, busque no texto a entidade de pessoa mais próxima usando spaCy.

Crie uma função de fallback: quando regex não encontrar o titular no contexto, a IA do spaCy deve sugerir a pessoa mais provável.

Crie um campo extra no banco de dados chamado origem_identificacao com os valores:

regex (se a palavra-chave estava no contexto)

ia_spacy (se a IA encontrou o titular)

nao_identificado (se nenhum método encontrou)

Adicione um filtro no dashboard Streamlit para visualizar os registros baseados no método de identificação.

Dependências adicionais:
spacy

pt_core_news_lg (use python -m spacy download pt_core_news_lg)

Opcional:
Se possível no Replit, integre com LangChain + OpenAI (via API Key) para análise semântica profunda nos casos onde a regex e o spaCy não resolverem.

Fluxo com IA:
plaintext
Copiar
Editar
Arquivo → Extração de texto → Regex → Busca de titular por contexto → Se falhar → Busca com spaCy NER → Classificação → Gravação no banco → Dashboard
IA Aplicada:
Nomear automaticamente os titulares mesmo sem padrão.

Adaptar a busca para documentos sem estrutura, como contratos complexos, e-mails, assinaturas soltas.

Priorizar dados críticos com apoio semântico.

Relatar no dashboard a confiança de cada método de captura.